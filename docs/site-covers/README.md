# site-covers 原檔（只作參考，不要直接使用）

來自 `site-covers.zip`（已解壓到 `assets/covers/`、`assets/og/`，zip 已刪除）。

- `writing-snippet.html`：/writing 書架片段原檔。
- `writing-jsonld.original.json`：JSON-LD 原檔。**不可直接貼上**——內含 `bookFormat`（未確認版本），
  並把仍在寫作中的《拾味小館》標成有出版者及 Paperback。
  已合併的版本見 `preview/writing-v2.html` 的 `<head>`，合併規則見 `DESIGN_PROPOSAL.md` §8.2。
